import "test";

void main() {
    var x int;

    x = ---5 * (4 + 3 + 2 * (5 + 1)) / 10 * 12 - 3 * (((2)-1)-1);
    
    if (x == -108) {
        test:pass();
    } else {
        test:fail();
    }
}
