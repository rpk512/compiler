import "test";

void main() {
    if (False || True || test:fail()) {
    
    }

    if (True || test:fail()) {
        
    }

    if (False || False || False) {
        test:fail();
    }

    test:pass();
}
