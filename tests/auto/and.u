extern void print_line(string s);
extern void exit(int n);

bool fail() {
    print_line("FAIL");
    exit(1);
    return True;
}

void main() {
    if (True && False && fail()) {
    
    }

    if (True && True && True) {

    } else {
        fail();
    }

    print_line("PASS");
}
