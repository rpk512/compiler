extern void print_line(string s);

void main() {
    var i int;
    var j int;
    var n int;

    n = 0;
    i = 0;
    while (i < 4) {
        j = 0;
        while (j < 3) {
            j = j + 1;
            n = n + 1;
        }
        i = i + 1;
    }

    if (i == 4 && n == 12) {
        print_line("PASS");
    } else {
        print_line("FAIL");
    }
}
